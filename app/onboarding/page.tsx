'use client';
import {useState} from 'react';
import {supabaseBrowser} from '@/lib/supabase-browser';
import {uniqueSlug} from '@/lib/slug';
export default function Onboarding(){const [name,setName]=useState(''),[whatsapp,setWhatsapp]=useState(''),[busy,setBusy]=useState(false);async function save(){setBusy(true);const supabase=supabaseBrowser();const {data:{user}}=await supabase.auth.getUser();if(!user){setBusy(false);return location.href='/login'}const slug=uniqueSlug(name);const {data:biz,error}=await supabase.from('businesses').insert({owner_id:user.id,name,slug,whatsapp,subscription_status:'pending_verification'}).select('id').single();if(error){setBusy(false);return alert(error.message);}const act=await fetch('/api/onboarding/activate',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({userId:user.id,businessId:biz.id})});const j=await act.json();setBusy(false);if(!act.ok)return alert(j.error||'Pagamento ainda não confirmado. Tente novamente em alguns segundos.');location.href='/admin'}return <main className="authPage"><section className="authCard"><span className="eyebrowM">ÚLTIMO PASSO</span><h1>Configure seu estabelecimento</h1><p>O nome público pode se repetir. O LG Agenda cria um identificador único por estabelecimento para evitar qualquer conflito.</p><label>Nome do estabelecimento<input value={name} onChange={e=>setName(e.target.value)} placeholder="Ex.: Lopes Barbearia"/></label><label>WhatsApp do estabelecimento<input value={whatsapp} onChange={e=>setWhatsapp(e.target.value)} placeholder="11999999999"/></label><button className="checkoutPrimary" disabled={!name||busy} onClick={save}>{busy?'Salvando...':'Criar meu estabelecimento →'}</button></section></main>}
